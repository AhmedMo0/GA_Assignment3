/*
************************** Note: this for testing only remove main method when running the the app to avoid errors **************************

*/
import java.util.ArrayList;
import java.util.Arrays;
public class Main{
	
	/*
	public static void main(String[] args) {
			    
		FuzzySet s1 = new FuzzySet("small", "TRAP", new ArrayList<>(Arrays.asList(0,0,20,40)));
		FuzzySet s2 = new FuzzySet("ordinary", "TRAP", new ArrayList<>(Arrays.asList(20,40, 60, 80)));
		
		s1.calcMemValue(50);
		System.out.println(s1);
		
		s2.calcMemValue(50);
		System.out.println(s2);
		
		

	}
	
	*/

}
